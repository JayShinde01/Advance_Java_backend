# Server
server.port=8080


# Database (TiDB Cloud)
spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/springboot?useSSL=true&sslMode=REQUIRED&serverTimezone=UTC}
spring.datasource.username=${DB_USER:4SowHijt51qstCi.root}
spring.datasource.password=${DB_PASSWORD:s9i2xjAWdVReT0Vc}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# ✅ Use MySQL8Dialect for TiDB compatibility
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Secret (auto-injected from Render env vars)
jwt.secret=${JWT_SECRET:TXlTdXBlclNlY3JldEtleTEyMzQ1IQ==}

